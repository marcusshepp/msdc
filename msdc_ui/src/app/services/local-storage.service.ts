import { Injectable } from "@angular/core";
import { Observable } from "rxjs";
import { PointerUser } from "../pointer/pointer.models";

@Injectable({providedIn: 'root'})
export class LocalStorageService {
}